
PORT=5000
NODE_ENV=development

DATABASE_URL=postgresql://postgres:vinod@5051@localhost:5432/quibly

JWT_SECRET=your_jwt_secret_here_change_in_production

# Multiple frontend URLs (comma-separated)
# Example: http://localhost:3000,http://192.168.1.5:3000,https://yourdomain.com
FRONTEND_URL=http://localhost:3000

# For Docker (local development):
REDIS_STRING=localhost
REDIS_PORT_NO=6379
REDIS_PASSWORD=redis_password

# For Docker (local development):
KAFKA_BROKERS=localhost:9092

# ============================================
# DISTRIBUTED SYSTEM CONFIGURATION
# ============================================
# Unique server identifier for multi-server deployments
# If not set, will auto-generate: server_<timestamp>_<random>
# In production, set this to identify each server instance:
# SERVER_ID=server-1
# SERVER_ID=server-2
# etc.
SERVER_ID=

CLOUDINARY_CLOUD_NAME='dibonenql'
CLOUDINARY_API_KEY='492472349955474'
CLOUDINARY_API_SECRET='1RJkIjYYtfAfrcqvxeKTG57XUNw'

EMAIL_USER=your_email@gmail.com
EMAIL_PASS=your_app_password

LIVEKIT_API_KEY=your_livekit_api_key
LIVEKIT_API_SECRET=your_livekit_api_secret
LIVEKIT_WS_URL=wss://your-project.livekit.cloud

GOOGLE_CLIENT_ID=your_google_client_id

# ============================================
# DOCKER-SPECIFIC NOTES
# ============================================
# When running with docker-compose, use these settings:
#
# DATABASE_URL=postgresql://postgres:postgres@postgres:5432/quibly
# REDIS_STRING=redis
# REDIS_PORT_NO=6379
# REDIS_PASSWORD=redis_password
# KAFKA_BROKERS=kafka:29092
#
# Note: Use service names (postgres, redis, kafka) instead of localhost
# when services communicate within Docker network

# ============================================
# PRODUCTION DEPLOYMENT NOTES
# ============================================
# For multi-server production deployment:
#
# 1. Load Balancer: Configure sticky sessions (session affinity)
#    - Use cookie-based or IP hash routing
#    - Ensures WebSocket reconnections go to same server
#
# 2. Redis Cluster: Use managed Redis (AWS ElastiCache, Redis Cloud)
#    - Set REDIS_STRING to cluster endpoint
#    - Enable automatic failover
#
# 3. Kafka Cluster: Use managed Kafka (Confluent Cloud, AWS MSK)
#    - Set KAFKA_BROKERS to cluster endpoints
#    - Configure replication factor >= 3
#
# 4. Health Checks: Point load balancer to /health endpoint
#    - Returns 200 if healthy, 503 if degraded
#    - Includes service status (Redis, Kafka, DB, Socket.IO)
#
# 5. Server IDs: Set unique SERVER_ID for each instance
#    - Example: SERVER_ID=us-east-1a-server-1
#    - Helps with debugging and monitoring
